export interface Profile {
  id?: number;
  username: string;
  password: string;
  active: number;
  ip: string;
  lastran: string;
  laststart: string;
  status: number;
  like: number;
  follow: number;
}
